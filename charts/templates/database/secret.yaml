{{- if eq .Values.database.type "internal" -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "vdp.database" . }}
  labels:
    {{- include "vdp.labels" . | nindent 4 }}
type: Opaque
data:
  POSTGRES_PASSWORD: {{ template "vdp.database.encryptedPassword" . }}
{{- end -}}
