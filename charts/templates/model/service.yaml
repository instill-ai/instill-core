apiVersion: v1
kind: Service
metadata:
  name: {{ template "vdp.model" . }}
  labels:
    {{- include "vdp.labels" . | nindent 4 }}
    app.kubernetes.io/component: model
  {{- with .Values.model.serviceAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if (eq .Values.expose.ingress.controller "gce") }}
  type: NodePort
  {{- end }}
  ports:
    - name: {{ ternary "https" "http" .Values.internalTLS.enabled }}
      port: {{ template "vdp.model.port" . }}
      targetPort: {{ template "vdp.model.port" . }}
  selector:
    {{- include "vdp.matchLabels" . | nindent 4 }}
    app.kubernetes.io/component: model
