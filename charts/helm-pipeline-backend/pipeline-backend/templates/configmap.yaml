apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pipeline-backend.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "pipeline-backend.labels" . | indent 4 }}
data:
  # Render the pipeline-backend config
  config.yaml: |
    {{- toYaml .Values.deployment.config | nindent 4 }}
