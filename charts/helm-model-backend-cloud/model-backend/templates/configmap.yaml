apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "model-backend.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "model-backend.labels" . | indent 4 }}
data:
  # Render the model-backend config
  config.yaml: |
    {{- toYaml .Values.deployment.config | nindent 4 }}
