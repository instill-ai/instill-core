{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: {{ include "model-backend.fullname" . }}
  labels:
    {{- include "model-backend.labels" . | nindent 4 }}
spec:
  {{- toYaml .Values.podDisruptionBudget.spec | nindent 2 }}
  selector:
    matchLabels:
      {{- include "model-backend.selectorLabels" . | nindent 6 }}
{{- end }}
