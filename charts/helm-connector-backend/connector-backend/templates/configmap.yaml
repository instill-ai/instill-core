apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "connector-backend.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "connector-backend.labels" . | indent 4 }}
data:
  # Render the connector-backend config
  config.yaml: |
    {{- toYaml .Values.deployment.config | nindent 4 }}
