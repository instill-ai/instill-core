apiVersion: v1
kind: Service
metadata:
  name: {{ include "connector-backend.fullname" . }}
  labels:
    {{- include "connector-backend.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.deployment.config.server.port }}
    {{- if and .Values.deployment.config.server.https.cert .Values.deployment.config.server.https.key }}
      targetPort: https
      name: https
    {{- else }}
      targetPort: http
      name: http
    {{- end }}
      protocol: TCP
  selector:
    {{- include "connector-backend.selectorLabels" . | nindent 4 }}
