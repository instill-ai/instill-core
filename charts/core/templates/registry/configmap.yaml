apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "core.registry" . }}
  labels:
    {{- include "core.labels" . | nindent 4 }}
    app.kubernetes.io/component: registry
data:
  config.yaml: |-
{{ toYaml .Values.registry.config}}
