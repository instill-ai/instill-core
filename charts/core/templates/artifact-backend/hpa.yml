{{- if .Values.artifactBackend.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ template "core.artifactBackend" . }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    {{- if .Values.tags.argoRollout }}
    kind: Rollout
    {{- else }}
    kind: Deployment
    {{- end }}
    name: {{ template "core.artifactBackend" . }}
    labels:
    {{- include "core.labels" . | nindent 4 }}
    app.kubernetes.io/component: artifact-backend
  minReplicas: {{ .Values.artifactBackend.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.artifactBackend.autoscaling.maxReplicas }}
  metrics:
{{- with .Values.artifactBackend.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ . }}
{{- end }}
{{- with .Values.artifactBackend.autoscaling.targetAverageMemoryUtilization }}
    - type: Resource
      resource:
        name: memory
        target:
          type: AverageValue
          averageValue: {{ . }}
{{- end }}
{{- end }}
