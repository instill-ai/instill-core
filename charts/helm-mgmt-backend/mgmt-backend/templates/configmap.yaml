apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mgmt-backend.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "mgmt-backend.labels" . | indent 4 }}
data:
  # Render the mgmt-backend config
  config.yaml: |
    {{- toYaml .Values.deployment.config | nindent 4 }}
