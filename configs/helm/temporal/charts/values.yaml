server:
  replicaCount: 1
  config:
    namespaces:
      create: true
      namespace:
        - name: instill-core
          retention: 30d

cassandra:
  config:
    cluster_size: 1

elasticsearch:
  replicas: 1

prometheus:
  enabled: false

grafana:
  enabled: false
