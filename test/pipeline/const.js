export const apiHost = `${__ENV.HOST}`

// Read a token from the environment variables
export const slackToken = `${__ENV.TOKEN}`

export const dogImg = open(`${__ENV.TEST_FOLDER_ABS_PATH}/pipeline/data/dog.jpg`, "b");
export const bearImg = open(`${__ENV.TEST_FOLDER_ABS_PATH}/pipeline/data/bear.jpg`, "b");

export const streetImg = open(`${__ENV.TEST_FOLDER_ABS_PATH}/pipeline/data/street.png`, "b");
export const street2Img = open(`${__ENV.TEST_FOLDER_ABS_PATH}/pipeline/data/street2.png`, "b");

export const danceImg = open(`${__ENV.TEST_FOLDER_ABS_PATH}/pipeline/data/dance.jpg`, "b");
export const dwaynejohnsonImg = open(`${__ENV.TEST_FOLDER_ABS_PATH}/pipeline/data/dwaynejohnson.jpeg`, "b");

export const signsmallImg = open(`${__ENV.TEST_FOLDER_ABS_PATH}/pipeline/data/sign-small.jpg`, "b");
export const signpostImg = open(`${__ENV.TEST_FOLDER_ABS_PATH}/pipeline/data/emergency-evacuation-route-signpost.jpg`, "b");
