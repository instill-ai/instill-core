# docker compose project name
COMPOSE_PROJECT_NAME=instill-vdp

# container build
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# usage collection flag
DISABLEUSAGE=false

# version
GOLANG_VERSION=1.19.3
ALPINE_VERSION=3.16
KRAKEND_VERSION=2.1.3

# api-gateway
API_GATEWAY_VERSION=latest
API_GATEWAY_HOST=api-gateway
API_GATEWAY_PORT=8080

# pipeline-backend
PIPELINE_BACKEND_VERSION=0.9.3-alpha
PIPELINE_BACKEND_HOST=pipeline-backend
PIPELINE_BACKEND_PORT=8081

# connector-backend
CONNECTOR_BACKEND_VERSION=0.7.2-alpha
CONNECTOR_BACKEND_HOST=connector-backend
CONNECTOR_BACKEND_PORT=8082

# model-backend
MODEL_BACKEND_VERSION=0.10.0-alpha
MODEL_BACKEND_HOST=model-backend
MODEL_BACKEND_PORT=8083

# mgmt-backend
MGMT_BACKEND_VERSION=0.2.7-alpha
MGMT_BACKEND_HOST=mgmt-backend
MGMT_BACKEND_PORT=8084

# console
CONSOLE_VERSION=0.14.0-alpha
CONSOLE_HOST=console
CONSOLE_PORT=3000

# triton-server
TRITON_SERVER_VERSION=22.05
TRITON_SERVER_HOST=triton-server
TRITON_SERVER_PORT=8001

# triton-conda-env
TRITON_CONDA_ENV_VERSION=0.2.6-alpha

# PostgreSQL
POSTGRESQL_VERSION=14.1
POSTGRESQL_HOST=pg-sql
POSTGRESQL_PORT=5432

# Elasticseach
ELASTICSEARCH_VERSION=7.16.2
ELASTICSEARCH_HOST=elasticsearch
ELASTICSEARCH_PORT=9200

# Temopral
TEMPORAL_VERSION=1.17.1
TEMPORAL_HOST=temporal
TEMPORAL_PORT=7233

TEMPORAL_UI_VERSION=2.3.0
TEMPORAL_UI_HOST=temporal-ui
TEMPORAL_UI_PORT=8088

# Redis
REDIS_VERSION=7.0
REDIS_HOST=redis
REDIS_PORT=6379

# Redoc OpenAPI
REDOC_OPENAPI_VERSION=v2.0.0-rc.70
REDOC_OPENAPI_HOST=redoc-openapi
REDOC_OPENAPI_PORT=3001

# This flag is used for integration test in which dummy model is used instead of pulling model from GitHub, HuggingFace or ArtiVC.
# The reason is reducing the impact of network trouble during integration test
# The default value is alway false, only set when running `make integration-test`
ITMODE=false

# Max image file size in MB which pipeline and model backend accept to process
MAX_IMAGE_SIZE=12
# Elasticseach
ELASTICSEARCH_VERSION=7.16.2
